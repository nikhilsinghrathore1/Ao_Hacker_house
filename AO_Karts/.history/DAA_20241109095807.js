// ṭo reverse the stack using recursion is js


let arr = [1,2,3,4,5,6] ; // this is an stack 

console.log(arr.length)
// now have to reverse it 
               

const rev = (arr)=>{
               i = 0 
               while(i!=arr.length){
                              
               }
               
}